{
  "name": "planning-algorithms-explorer",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root for Planning Algorithms Explorer",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "frontend:install": "npm --prefix frontend install",
    "frontend:dev": "npm --prefix frontend run dev"
  }
}
